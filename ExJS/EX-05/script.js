var width;
var height;
var area;

width = Number(prompt("Type the width of the area"));
height = Number(prompt("Type the height of the area"));

area = width * height;

alert(`The area of the property is ${area}`);